// Package main is the dev database seeder.
// It will be implemented alongside the domain models in Phase 1.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "seed: not yet implemented — will be wired up in Phase 1")
	os.Exit(1)
}
